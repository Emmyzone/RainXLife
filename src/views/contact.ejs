<%- include('partials/header') %>

<section class="container static-page">
  <h1>Contact</h1>
  <p>Have a book suggestion, a correction, or a partnership question? Reach out anytime.</p>
  <p>Email: <a href="mailto:hello@rainxlife.com">hello@rainxlife.com</a></p>
  <p>You can also find RainXLife on
    <% if (tiktokUrl) { %><a href="<%= tiktokUrl %>" target="_blank" rel="noopener">TikTok</a><% } %>
    <% if (youtubeUrl) { %> and <a href="<%= youtubeUrl %>" target="_blank" rel="noopener">YouTube</a>.<% } %>
  </p>
  <p class="empty-state">(Replace the email above with your real contact address, or wire this page up to a contact form provider later.)</p>
</section>

<%- include('partials/footer') %>
